export function SummaryField({type , score}){
    return (
        <div>
            <img src="" alt="" />
            <span>{type}</span>

            <strong> {score} /100 </strong>
        </div>
    )
}